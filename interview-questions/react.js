const [counter,setCounter] = useState(0)
const func_inc = ()=>{
    setCounter(counter+1)
    setCounter(counter+1)
    setCounter(counter+1)
    setCounter(counter+1)
}

// op will be 1

const [counter,setCounter] = useState(0)
     func_inc = ()=>{
    setCounter(prec_counter =>{prec_counter+1})
    setCounter(prec_counter =>{prec_counter+1})
    setCounter(prec_counter =>{prec_counter+1})
    setCounter(prec_counter =>{prec_counter+1})
}

// op will be 4 

// 1. ---> We dont use return the setCounter values 